# Database Configuration
# Für LOCAL MySQL:
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=flutterflex_db

# Für Docker MySQL (docker-compose):
# DB_HOST=db
# DB_USER=flutter_user
# DB_PASSWORD=userpassword123
# DB_NAME=flutterflex_db

# Server Configuration
PORT=3000

# JWT Secret (ÄNDERN in Production!)
# Generiere mit: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change_this_to_a_super_secure_random_string_in_production
